#include<stdio.h>
int main()
{
    int seta[10], setb[10], na, nb, i;
    printf("Enter the no. of elements in Set A: ");
    scanf("%d", &na);
    printf("Enter the no. of elements in Set B: ");
    scanf("%d", &nb);
    printf("Enter the elements for Set A: ")
    for(i = 0; i < na; i++)
    {
        scanf()
    }

}